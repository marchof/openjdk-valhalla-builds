name: OpenJDK Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        repository: openjdk/valhalla
        ref: lw5
    - name: Build
      run: |
        bash configure
        make images
